# LLM과 Transformer 아키텍처 - Day 1·Day 2 상세 정리

> 페이지 표기는 모두 **PDF 페이지 기준**이다. 슬라이드 하단 번호보다 PDF 페이지가 1 큰 경우가 많다.
>
> Day 2의 모델 순위·제품 버전·시장 수치는 자료가 캡처된 시점의 스냅샷이다. 시간이 지나면 바뀌므로 원리와 평가 관점을 중심으로 학습하는 것이 좋다.

## 0. 전체 흐름

두 자료를 한 문장으로 연결하면 다음과 같다.

> **Day 1은 언어를 벡터로 바꾸고 Transformer가 문맥을 계산하는 원리를 설명하고, Day 2는 그 Transformer를 대규모로 학습·정렬·경량화하여 실제 업무에 쓰는 방법을 설명한다.**

| 구분 | 핵심 질문 | 주요 내용 |
|---|---|---|
| Day 1 | LLM은 문장을 어떻게 이해하고 다음 토큰을 만드는가? | Software 3.0, NLP 표현의 발전, 임베딩, 언어모델, Attention, Transformer |
| Day 2 | 그렇게 만든 LLM을 어떻게 더 유용하고 크고 효율적으로 만들며 업무에 적용하는가? | 생산성·직무 변화, LLM 평가, 주요 모델 생태계, RLHF, Scaling Law, Distillation, MoE |

핵심 연결 구조는 다음과 같다.

`텍스트 → 토큰/벡터 → Self-Attention → 문맥 표현 → 다음 토큰 예측 → 대규모 사전학습 → 사람 선호 정렬 → 경량화/효율화 → Agent·업무 시스템`

---

## 1. Day 1 - Transformer의 기반 원리

### 1.1 Software 1.0 → 2.0 → 3.0

자료는 소프트웨어 개발 패러다임을 세 단계로 설명한다. (Day 1, pp.4-15)

| 구분 | 사람이 제공하는 것 | 기계가 만드는 것 | 특징/한계 |
|---|---|---|---|
| Software 1.0 | 명시적인 규칙과 코드 | 실행 프로그램 | 동작을 예측·추적하기 쉽지만 복잡한 판단 규칙을 사람이 모두 작성해야 함 |
| Software 2.0 | 데이터, 레이블, 모델 구조, 학습 절차 | 학습된 가중치 | 사람이 규칙을 쓰지 않아도 되지만 보통 문제마다 별도 모델·데이터·학습·배포가 필요 |
| Software 3.0 | 자연어 목표, 역할, 제약, 데이터·도구 | 코드, 추론, 행동 계획, Agent 작업 | 자연어가 프로그래밍 인터페이스가 되고 범용 모델을 여러 업무에 재사용 |

자료가 말하는 Software 3.0의 핵심은 “코드를 직접 전부 작성하는 것”에서 “지능이 일할 목표와 환경을 설계하는 것”으로 개발자의 역할이 이동한다는 점이다. 자연어·프롬프트·모델·컨텍스트가 기존의 코드·함수·프로그램과 대응한다. LLM은 단순 챗봇이 아니라 추론 엔진, 지식 인터페이스, 자연어 컴파일러, Agent의 두뇌를 겸하는 실행환경으로 설명된다. (pp.9-13)

다만 Software 3.0에도 다음 한계가 그대로 남는다. (p.14)

- **Black box**: 수많은 파라미터가 특정 답을 만든 원인을 완전히 설명하기 어렵다.
- **Bias**: 학습 데이터의 문화적·사회적 편향과 낡거나 틀린 정보가 반영될 수 있다.
- **Hallucination**: 모델은 사실을 검색하는 장치가 아니라 그럴듯한 다음 토큰을 예측하므로 가짜 출처·수치·코드를 자연스럽게 만들 수 있다.
- **Non-determinism**: 같은 입력이라도 샘플링과 문맥 차이에 따라 결과가 달라질 수 있다.

따라서 실무 시스템은 다음처럼 역할을 나누는 것이 안전하다.

- 규칙 준수, 금액 계산, 권한 검사, 데이터 저장처럼 **정확성과 재현성이 필요한 부분은 전통 코드**가 담당한다.
- 요약, 분류, 초안, 판단 보조, 계획 수립처럼 **언어·추론이 필요한 부분은 LLM**이 담당한다.
- 최종 승인, 책임 판단, 위험한 행동은 **사람 또는 결정론적 검증 계층**이 담당한다.

이것이 자료 후반의 **증강지능(Augmented Intelligence)** 관점이다. AI의 목표를 인간 대체가 아니라 인간의 판단·생산성·문제 해결 범위를 넓히는 것으로 본다. (p.24)

### 1.2 언어를 숫자로 바꾸는 방법의 발전

기계는 문자열 자체가 아니라 수치 벡터를 계산한다. Day 1의 NLP 부분은 “표현을 어떻게 만들 것인가”의 역사로 읽으면 쉽다. (pp.25-43)

#### 1) Corpus와 Vocabulary

- **Corpus**: 학습·분석을 위해 수집한 언어 데이터 전체
- **Vocabulary**: 해당 작업에서 모델이 구분하는 단어 또는 토큰의 집합

Vocabulary가 50,000개라면 각 단어를 구분하는 기본 표현도 50,000차원이 될 수 있다. 여기서 희소성·차원 증가 문제가 시작된다. (p.26)

#### 2) One-hot과 Bag of Words

- **One-hot vector**: 한 단어에 해당하는 위치만 1이고 나머지는 0인 벡터
- **BoW(Bag of Words)**: 문장을 Vocabulary별 출현 횟수로 표현한 벡터

예를 들어 Vocabulary가 `(여기, 지금, 아주, 위험, 합니다)`이고 문장이 “아주 아주 위험 합니다”라면 BoW는 `(0, 0, 2, 1, 1)`이 된다. 사용된 단어와 빈도는 알 수 있지만 순서는 사라진다. (p.27)

#### 3) N-gram과 TF-IDF

**N-gram**은 연속된 n개 토큰을 하나의 단위로 보아 일부 순서를 보존한다. Bigram이면 `not good`과 `good not`을 구분할 수 있다. 하지만 n이 커질수록 가능한 조합과 Vocabulary가 급격히 증가한다. (p.28)

**TF-IDF**는 “이 문서에서는 자주 나오지만 다른 문서에서는 드문 단어”를 중요하게 본다.

\[
TF(t,d)=\frac{\text{문서 }d\text{에서 }t\text{의 출현 횟수}}{\text{문서 }d\text{의 전체 단어 수}}
\]

\[
IDF(t,D)=\log\frac{\text{전체 문서 수}}{\text{단어 }t\text{가 들어 있는 문서 수}}
\]

\[
TF\text{-}IDF(t,d,D)=TF(t,d)\times IDF(t,D)
\]

TF가 “문서 내부 중요도”라면 IDF는 “전체 문서에서의 희소성”이다.

#### 4) 빈도 기반 표현의 한계

- 어순과 긴 문맥을 충분히 표현하지 못한다.
- Vocabulary가 커지면 벡터가 매우 크고 대부분 0인 고차원 희소 벡터가 된다.
- `동생`과 `아우`, `사과`와 `오렌지`처럼 의미 관계를 직접 나타내지 못한다.
- 새 단어나 표현 변화에 유연하게 대응하기 어렵다. (p.29)

WordNet 같은 Thesaurus는 사람이 Synset, Hypernym, Hyponym, Meronym 등의 의미 관계를 그래프로 정의하려 한 접근이다. 명시적 관계를 제공하지만 구축 비용이 크고 신조어·의미 변화를 계속 반영하기 어렵다. (p.30)

### 1.3 분포가설과 Word Embedding

분포가설의 핵심은 **“비슷한 문맥에서 등장하는 단어는 의미도 비슷하다”**이다. `아우`와 `동생` 주위에 `형제`, `부모`, `나이` 같은 단어가 비슷하게 등장한다면 두 단어를 가까운 의미로 학습할 수 있다. (p.31)

Word2Vec은 주변 단어와 중심 단어 사이의 예측 문제를 학습해, 의미 관계가 담긴 밀집 벡터를 얻는다. 학습이 진행될수록 비슷한 단어는 임베딩 공간에서 가까워지고, 일부 관계는 방향으로 나타난다. 자료의 `King - Man + Woman ≈ Queen`, 국가-수도 예시가 이를 설명한다. (pp.32-35)

중요한 구분은 다음과 같다.

| 표현 | 같은 단어의 벡터 | 문맥 반영 |
|---|---|---|
| Word2Vec/GloVe 같은 정적 임베딩 | 항상 동일 | `bank`가 은행인지 강둑인지 직접 구분 못함 |
| Transformer의 문맥 임베딩 | 문장마다 달라짐 | 주변 토큰을 Attention으로 반영해 의미가 달라짐 |

즉, 현대 Transformer의 출력은 “단어 사전에서 꺼낸 고정 벡터”가 아니라 **그 문장 안에서 다시 계산된 문맥 벡터**다. (p.40)

### 1.4 Language Model → RNN → Attention → Transformer

**Language Model(LM)**은 단어 시퀀스에 확률을 부여하고 이전 토큰을 바탕으로 다음 토큰의 분포를 예측한다. (p.36)

\[
P(w_1,\ldots,w_T)=\prod_{t=1}^{T}P(w_t\mid w_1,\ldots,w_{t-1})
\]

이 정의가 GPT류 모델의 핵심과 직접 연결된다.

| 단계 | 아이디어 | 해결한 문제 | 남은 한계 |
|---|---|---|---|
| RNN | hidden state에 과거 정보를 누적 | 순차 데이터 모델링 | 순차 계산으로 병렬화가 어렵고 vanishing gradient로 긴 의존성 학습이 약함 |
| LSTM/GRU | Gate로 기억·삭제·출력을 제어 | 장기 정보 보존 개선 | 구조와 연산이 복잡하며 여전히 순차 처리 |
| 초기 Seq2Seq | Encoder 문장을 하나의 context vector로 압축해 Decoder에 전달 | 입력과 출력 길이가 다른 번역 등 | 긴 문장을 한 벡터에 압축해 정보 병목 발생 |
| Attention | 출력 시점마다 Encoder의 모든 상태를 보고 중요한 부분을 가중합 | 고정 context 병목 완화 | RNN과 결합된 경우 순차 처리 한계가 남음 |
| Transformer | RNN/CNN 없이 Self-Attention 중심으로 전체 토큰 관계를 계산 | 장기 의존성과 학습 병렬화 개선 | Attention 비용, 위치 정보 부재, 대규모 학습 비용 |

Transfer Learning은 대규모 데이터로 먼저 표현을 학습한 Pretrained Model을 여러 다운스트림 작업에 재사용하는 방식이다. Transformer의 병렬 학습 가능성과 대규모 데이터·연산이 결합하면서 GPT 같은 범용 사전학습 모델이 등장한다. (pp.41-43)

### 1.5 Transformer를 위한 수학

#### Euclidean distance

\[
d(x,y)=\sqrt{\sum_i(x_i-y_i)^2}
\]

두 벡터의 절대적인 위치·크기 차이를 반영한다. (p.46)

#### Cosine similarity

\[
\cos\theta=\frac{x\cdot y}{\|x\|\|y\|}
\]

벡터 길이를 정규화하고 방향이 얼마나 같은지 본다. 같은 방향이면 1, 직교하면 0, 반대 방향이면 -1이다. 단어 빈도가 모두 음이 아닌 일반적인 문서 벡터에서는 보통 0에서 1 사이가 된다. (pp.47-49)

자료의 문서 비교 예시는 두 척도의 차이를 잘 보여준다. (p.50)

- 문서 1: `(강아지, 고양이, 토끼) = (1,1,0)`
- 문서 2: `(1,1,1)`
- 문서 3: `(3,3,0)`

검산 결과:

- Euclidean: `d(문서1, 문서2)=1`, `d(문서1, 문서3)=√8≈2.828` → 문서 2가 더 가깝다.
- Cosine: `sim(문서1, 문서2)=2/(√2·√3)≈0.816`, `sim(문서1, 문서3)=1` → 문서 3이 더 유사하다.

문서 3은 문서 1보다 길지만 단어 비율과 방향은 같다. 그래서 **절대 빈도 차이를 보는 Euclidean**과 **주제·비율 방향을 보는 Cosine**이 다른 결론을 낸다.

#### Dot product와 Matrix multiplication

\[
a\cdot b=\sum_i a_i b_i=\|a\|\|b\|\cos\theta
\]

내적은 두 벡터의 크기와 방향 정렬 정도를 함께 반영한다. Transformer에서는 Query와 Key의 관련도 점수를 만드는 핵심 연산이다. (pp.51-52)

행렬곱의 각 원소는 왼쪽 행과 오른쪽 열의 내적이다. 여러 토큰의 Q와 K를 한꺼번에 곱하는 `QK^T`도 같은 원리다. (p.53)

#### Linear transformation

\[
Y=XW
\]

학습된 Weight Matrix `W`는 입력 벡터를 새로운 표현 공간으로 투영한다. `X(8×10) × W(10×5) = Y(8×5)`라면 8개 토큰의 10차원 표현을 5차원으로 바꾼다. 딥러닝 학습은 W를 조정하여 데이터가 목적에 맞는 위치에 놓이도록 표현 공간을 재배치하는 과정이다. (pp.54-55)

#### Softmax

\[
Softmax(z_i)=\frac{e^{z_i}}{\sum_j e^{z_j}}
\]

임의의 실수 점수를 합이 1인 확률분포로 바꾼다. Attention에서 “각 토큰을 얼마나 참고할지”를 만들 때와 최종 다음 토큰 확률을 만들 때 모두 쓰인다. (p.56)

### 1.6 Self-Attention 단계별 이해

입력 토큰 표현을 `X`라고 하면 세 종류의 학습된 선형변환을 적용한다. (pp.62-66)

\[
Q=XW^Q,\qquad K=XW^K,\qquad V=XW^V
\]

- **Query**: 현재 토큰이 찾고 있는 정보, 즉 “누구를 참고할까?”
- **Key**: 각 토큰이 어떤 Query와 잘 맞는지를 나타내는 색인
- **Value**: 매칭된 뒤 실제로 전달·혼합되는 내용

전체 계산식은 다음과 같다.

\[
Attention(Q,K,V)=Softmax\left(\frac{QK^T}{\sqrt{d_k}}\right)V
\]

계산 순서:

1. **`QK^T`**: 모든 Query-Key 쌍의 내적을 계산해 `토큰 수 × 토큰 수` Score Matrix를 만든다.
2. **`÷√d_k`**: Key 차원이 커질수록 내적 값이 커지고 Softmax가 포화되는 문제를 줄인다.
3. **Mask(필요한 경우)**: 생성 모델에서는 미래 위치 점수를 매우 작은 값으로 만들어 보지 못하게 한다.
4. **Softmax**: 각 Query 행을 “어느 토큰을 얼마나 참고할지”에 대한 가중치로 바꾼다.
5. **`×V`**: Value 벡터를 가중합해 각 토큰의 새 문맥 표현을 만든다.

여기서 중요한 점은 Attention이 단순 유사도 계산으로 끝나지 않는다는 것이다. `QK^T`는 **읽을 위치를 결정**하고, `V`와의 곱은 **그 위치의 실제 정보를 가져온다.**

### 1.7 Multi-head와 Transformer block

**Multi-head Attention**은 서로 다른 `W^Q, W^K, W^V`를 가진 여러 Attention Head를 병렬 실행한다. 한 Head는 문법적 관계, 다른 Head는 의미 유사성, 또 다른 Head는 장거리 참조처럼 서로 다른 관계를 학습할 수 있다. 출력은 이어 붙인 뒤 최종 Linear layer로 재조합한다. (pp.67-68)

Transformer Encoder block의 핵심 구성은 다음과 같다. (pp.69-71)

- **Residual connection**: `입력 + 서브레이어 출력`. 원래 정보를 보존하고 깊은 네트워크의 gradient 흐름을 돕는다.
- **LayerNorm**: 토큰 벡터의 분포를 정규화해 학습을 안정화한다.
- **Feed Forward Network(FFN)**: Attention 뒤에 비선형 변환을 넣어 표현력을 높인다.
- **Positional Encoding**: Self-Attention만으로는 `나는 밥을 먹었다`와 `밥이 나를 먹었다`의 순서를 알 수 없으므로 위치 정보를 더한다.

Decoder의 **Masked Self-Attention**은 미래 토큰을 가린다. 학습 중 정답 문장 전체가 주어져도 각 위치는 자신보다 뒤의 정답을 볼 수 없다. 이 causal mask 덕분에 학습 방식과 실제 한 토큰씩 생성하는 방식이 일치한다. (pp.72-73)

---

## 2. Day 2 - Modern LLM과 실제 적용

### 2.1 생산성 혁명: 직업 삭제보다 업무 재구성

자료는 생성형 AI의 경제적 효과를 “지식 노동의 일부를 직접 수행하는 기술”로 설명한다. 글쓰기, 코딩, 분석, 조사, 문서화 같은 활동이 자동화되면서 직업 전체가 한 번에 사라지기보다 직업 안의 과업 조합이 바뀐다는 관점이다. 기업의 실제 가치는 모델 보유 자체보다 업무 흐름에 AI를 내재화할 때 생긴다. (Day 2, pp.4-8)

AlphaFold 사례는 AI가 특정 전공자만의 보조 도구가 아니라 서로 다른 분야의 경계를 넘는 문제 해결 수단이 될 수 있다는 메시지로 사용된다. 자료의 강조점은 특정 수치보다 **도메인 지식 + 데이터 + AI 모델링의 결합**이다. (pp.9-11)

### 2.2 Agent-first 개발과 FDE

자료는 개발 환경이 “사람이 코드를 쓰고 AI가 보조”하는 단계에서 “Agent가 계획·구현하고 사람이 감독”하는 단계로 이동한다고 설명한다. 이에 따라 경쟁력도 타이핑 속도보다 다음 능력으로 이동한다. (pp.12-18)

- 문제를 정확히 정의하고 성공 지표를 정하는 능력
- 백엔드·데이터·클라우드·보안 같은 기본 엔지니어링
- LLM, RAG, Tool calling, Multi-agent workflow를 설계하는 능력
- 현업 인터뷰를 통해 Pain point를 찾고 기술 요구사항으로 번역하는 능력
- 프로토타입을 빠르게 만들고 실제 시간·비용 절감 효과를 측정하는 능력

**FDE(Forward Deployed Engineer)**는 이 교집합에 있는 역할로 소개된다.

`현업 문제 발견 → 데이터 분석 → AI/Agent 설계 → 빠른 현장 적용 → 운영 안정화 → 효과 측정`

따라서 포트폴리오는 “어떤 모델을 만들었다”에서 끝나기보다 다음 스토리를 보여주는 것이 좋다.

1. 실제 사용자의 반복적·비효율적 문제를 정의한다.
2. 데이터를 탐색해 병목의 크기와 원인을 밝힌다.
3. LLM/Agent를 포함한 시스템을 설계한다.
4. 정확도, 처리 시간, 비용, 사용자 승인율 같은 지표로 결과를 평가한다.
5. 실패·환각·권한·개인정보 위험을 어떻게 통제했는지 설명한다.

### 2.3 현대 LLM의 공통 특징

현대 LLM은 대규모 파라미터, 대규모 텍스트 사전학습, Transformer, 여러 작업을 하나의 모델로 처리하는 범용성이 핵심이다. 사전학습으로 언어·지식·문맥 패턴을 얻고, Fine-tuning·Prompting·Alignment를 통해 실제 사용 목적에 맞춘다. (p.20)

대략적인 계층은 다음과 같다.

| 단계 | 무엇을 배우나 | 결과 |
|---|---|---|
| Pretraining | 방대한 텍스트에서 다음 토큰·언어 패턴 | 범용 지식과 언어 능력 |
| Fine-tuning/SFT | 특정 형식의 모범 입출력 | 지시 수행과 도메인 적응 |
| Preference alignment | 사람이 선호하는 응답 비교 | 더 유용하고 안전한 대화 행동 |
| Tool/RAG/Agent integration | 외부 지식·API·행동 루프 | 최신 정보 접근과 실제 업무 수행 |

### 2.4 LLM Leaderboard를 읽는 법

자료는 Stanford HELM, Vellum, Hugging Face Open LLM Leaderboard를 예로 든다. (pp.22-29)

- **HELM**: 정확도뿐 아니라 calibration, fairness, robustness, efficiency, toxicity 등 다면 평가에 강점
- **Vellum 계열 산업 리더보드**: 최신 상용 모델의 추론·수학·코딩·도구 사용·비용·속도 비교에 초점
- **Hugging Face Open LLM Leaderboard**: 공개 가중치 모델을 표준 데이터셋으로 비교하고 평가 결과를 공개

주요 지표의 의미:

- **GPQA Diamond**: 전문 과학 영역의 어려운 추론
- **AIME**: 수학 문제 해결
- **SWE-bench**: 실제 GitHub 이슈를 이해하고 코드 수정까지 수행하는 Agentic coding
- **Humanity's Last Exam**: 여러 전문 분야에 걸친 고난도 지식·추론
- **ARC-AGI**: 새로운 시각·추상 규칙을 파악하는 능력
- **MMMLU/MMLU**: 여러 분야·언어의 지식과 추론
- **Tool-use/Automation benchmark**: 도구를 호출하고 여러 단계를 끝까지 수행하는 능력

리더보드는 하나의 “종합 지능 점수”가 아니다. 실제 모델 선택에는 다음을 함께 봐야 한다.

- 자신의 데이터와 작업에서의 정확도
- 지연시간, 처리량, 토큰 비용
- Context window와 구조화 출력 안정성
- 한국어·도메인 성능
- Tool calling과 Agent loop 안정성
- 개인정보, 데이터 보존, 배포 위치, 라이선스

Day 2 pp.25-43의 순위와 모델 버전은 각 슬라이드 기준일의 캡처이므로 외워야 할 고정 순위가 아니다.

### 2.5 자료가 구분한 주요 모델 생태계

자료의 모델별 설명은 다음 포지셔닝을 전달한다. (pp.30-43)

| 생태계 | 자료가 강조한 방향 |
|---|---|
| GPT 계열 | 범용 추론·코딩, 빠른 모델과 깊은 추론 모델의 라우팅, 대중적 제품·API 생태계 |
| Gemini 계열 | 텍스트·이미지·음성·비디오·코드의 멀티모달 처리와 Google Workspace 연동 |
| Claude 계열 | 코드 생성·점진적 문제 해결·긴 문서 처리, 복잡한 작업의 사고 파트너 |
| Grok 계열 | 실시간 웹/X 데이터, 음성·비디오 상호작용 |
| LLaMA 계열 | 가중치 접근, 다양한 크기, Fine-tuning과 공개 생태계 중심 |

여기서 `오픈소스`와 `오픈웨이트`는 구분해야 한다. 가중치를 다운로드할 수 있어도 사용 규모·분야·재배포에 라이선스 제한이 있으면 전통적인 OSI 오픈소스와 같지 않다. 자료도 이를 `openwashing` 논점으로 언급한다. (p.40)

### 2.6 RLHF - 사람 선호에 맞추기

**RLHF(Reinforcement Learning from Human Feedback)**는 사전학습 모델이 단순히 다음 토큰을 잘 맞히는 것을 넘어, 사람이 더 유용하고 안전하며 의도에 맞는다고 평가하는 답을 생성하도록 정렬하는 과정이다. (pp.45-48)

고전적 RLHF 흐름:

1. **SFT(Supervised Fine-Tuning)**: 사람이 작성한 모범 답변으로 지시 수행 형식을 학습한다.
2. **Reward Model**: 같은 질문에 대한 여러 답변의 인간 선호 순위를 학습해 보상 점수를 예측한다.
3. **Reinforcement Learning**: 현재 LLM이 만든 답에 Reward Model의 점수를 주고, 높은 보상을 받도록 정책을 업데이트한다. 자료는 PPO를 예로 든다.

장점:

- 지시 이해와 응답 형식이 좋아진다.
- 정중함·일관성·안전성이 개선된다.
- 사용자가 실제로 도움 된다고 느끼는 답을 만들 가능성이 커진다.

한계:

- **Reward hacking**: 진짜로 좋은 답보다 보상 모델이 좋아할 말투·형식을 공략할 수 있다.
- **Sycophancy**: 사용자의 잘못된 판단에도 과도하게 동조하고 칭찬할 수 있다. (pp.49-51)
- 무난하고 안전한 답에 치우쳐 다양성과 창의성이 줄 수 있다.
- 평가자의 문화·가치관 편향이 Reward Model에 들어간다.
- 인간 라벨링 비용이 크다.

가장 중요한 보충은 **선호 정렬이 사실 정확성을 자동으로 보장하지는 않는다**는 점이다. 친절하고 자연스러운 환각도 가능하므로 Grounding, 출처, 검증이 별도로 필요하다.

### 2.7 Scaling Law - 크기·데이터·연산의 균형

Scaling Law는 모델 파라미터 `N`, 학습 데이터 토큰 `D`, 연산량 `C`를 늘리면 손실이 대체로 예측 가능한 Power Law 형태로 감소한다는 경험적 관찰이다. (pp.52-55)

\[
L(N)\propto N^{-\alpha},\qquad
L(D)\propto D^{-\beta},\qquad
L(C)\propto C^{-\gamma}
\]

이 관찰은 “새 아키텍처를 찾는 경쟁”뿐 아니라 더 큰 데이터·GPU·분산 학습 인프라를 확보하는 경쟁을 촉진했다.

하지만 **Chinchilla Scaling Law**는 무조건 파라미터만 키우는 것이 최적이 아니라고 보여준다. 정해진 연산 예산에서는 모델 크기와 학습 토큰을 균형 있게 늘려야 한다. 자료는 GPT-3의 큰 모델·상대적으로 적은 토큰과 Chinchilla의 더 작은 모델·더 많은 토큰을 대비한다. (p.56)

실무 해석:

- 모델이 너무 크고 데이터가 부족하면 파라미터를 충분히 학습하지 못한다.
- 데이터만 늘리고 모델이 너무 작으면 데이터 패턴을 흡수할 용량이 부족하다.
- 학습 비용뿐 아니라 서비스 시 추론 비용까지 고려하면 “가장 큰 모델”보다 “목표 성능을 가장 싸고 빠르게 내는 모델”이 더 좋을 수 있다.

### 2.8 Knowledge Distillation - 큰 모델의 능력을 작은 모델로

Distillation은 큰 **Teacher Model**의 출력·확률분포·추론 데이터를 사용해 작은 **Student Model**을 학습하는 지식 전달 방식이다. (pp.59-61)

자료의 흐름:

`Teacher가 문제·정답·설명 데이터를 생성 → 작은 공개 모델을 그 데이터로 Fine-tuning → 더 빠르고 저렴한 Student 모델`

두 레이블의 차이:

| Hard label | Soft label |
|---|---|
| 정답 클래스만 1인 One-hot | Teacher가 각 클래스에 부여한 확률분포 |
| “고양이”가 정답이라는 정보만 제공 | 고양이 0.7, 개 0.2, 여우 0.1처럼 클래스 간 유사성까지 제공 |
| 정보량이 적음 | Teacher의 판단 경계와 불확실성을 더 많이 전달 |

추론 과정 텍스트를 학습하는 방식도 넓은 의미의 지식 전달로 사용된다. 다만 “추론 문장을 복제했다”가 곧 Teacher의 내부 추론 메커니즘을 그대로 옮겼다는 뜻은 아니다. Student의 한계, 데이터 편향, Teacher의 오류도 함께 전파될 수 있다.

### 2.9 MoE - 필요한 전문가만 활성화

**Dense Model**은 매 토큰마다 모든 FFN 파라미터를 사용한다. **MoE(Mixture of Experts)**는 여러 Expert 중 Router가 토큰별 Top-k Expert만 선택한다. (pp.62-64)

Router의 개념적 계산:

\[
s=xW_r,\qquad p=Softmax(s)
\]

확률이 높은 Expert 몇 개를 선택하고 출력의 가중합을 만든다.

\[
y=\sum_{i\in TopK}p_iE_i(x)
\]

장점:

- 전체 파라미터 수는 크게 늘리면서 토큰당 활성 연산량은 제한할 수 있다.
- 서로 다른 입력이 서로 다른 Expert로 전달되며 전문화될 수 있다.
- 동일한 활성 연산 예산에서 모델 용량을 크게 만들 수 있다.

주의점:

- 특정 Expert에 토큰이 몰리는 load imbalance가 생길 수 있다.
- 분산 환경에서 Expert 간 통신 비용이 커질 수 있다.
- Router가 불안정하거나 일부 Expert가 충분히 학습되지 않는 문제가 생길 수 있다.
- **총 파라미터 수와 토큰당 활성 파라미터 수를 구분**해야 한다.

---

## 3. Day 1과 Day 2를 하나로 연결하기

### 3.1 한 번의 응답이 만들어지는 과정

1. 입력 문장을 토큰으로 나눈다.
2. 각 토큰을 Embedding vector로 바꾸고 위치 정보를 더한다.
3. 각 Transformer block에서 Q/K/V를 만들고 Self-Attention으로 문맥 정보를 섞는다.
4. Multi-head, Residual, LayerNorm, FFN을 반복해 깊은 문맥 표현을 만든다.
5. Decoder는 causal mask로 미래를 가린 상태에서 다음 토큰 logits를 만든다.
6. Softmax가 logits를 확률분포로 바꾸고 하나의 토큰을 선택한다.
7. 선택한 토큰을 입력 뒤에 붙여 같은 과정을 반복한다.

### 3.2 모델 능력과 행동·비용을 나누어 보기

| 질문 | 대응 기술 |
|---|---|
| 문맥을 어떻게 계산하는가? | Transformer Self-Attention |
| 범용 지식과 언어 능력을 어떻게 얻는가? | 대규모 Pretraining |
| 더 큰 능력을 어떻게 확보하는가? | Scaling Law와 데이터·연산 균형 |
| 사람 의도에 맞는 답을 어떻게 만들까? | SFT, RLHF 등 Alignment |
| 더 작은 모델에 능력을 어떻게 옮길까? | Knowledge Distillation |
| 큰 모델의 계산비를 어떻게 줄일까? | MoE의 sparse activation |
| 최신·사내 정보를 어떻게 연결할까? | RAG, Tool calling |
| 여러 단계의 실제 업무를 어떻게 끝낼까? | Agent workflow와 검증·관측 시스템 |

즉, **Transformer는 계산 구조**, **Pretraining은 능력 형성**, **RLHF는 행동 정렬**, **Distillation과 MoE는 효율화**, **RAG/Agent는 외부 세계와의 연결**이다.

---

## 4. 자료에서 꼭 주의할 부분

1. **`1 - Similarity = Distance`는 일반 법칙이 아니다.** 정규화된 유사도로 별도의 거리를 정의할 때 가능한 관계이며 Euclidean distance와 자동으로 같아지지 않는다. (Day 1 p.45)
2. BoW/One-hot의 차원을 직접 결정하는 것은 Corpus의 전체 토큰 수보다 **Vocabulary 크기**다. (Day 1 p.29 보충)
3. Day 1 p.54의 선형변환 예제는 표시된 `x`와 `W`를 곱한 결과가 슬라이드의 `y`와 일치하지 않는다. 상단 예시는 실제로 `[4,1]`이어야 하는데 `[2,1]`로 표시된다. 하단 예시도 수치가 일치하지 않아 개념·차원 설명용으로 보는 것이 안전하다.
4. Day 1 p.56의 Softmax 예시 `[2.1,1.3,0.2,-0.5]`의 정확한 값은 약 `[0.598,0.269,0.089,0.044]`다. 슬라이드 값 `[0.58,0.26,0.09,0.07]`은 합은 1이지만 정확 계산과 차이가 있다.
5. Day 2 p.61의 Soft Label 숫자 예시는 표시된 값들의 합이 1을 넘으므로 유효한 확률분포가 아니다. Soft Label의 개념은 맞지만 숫자는 오탈자로 봐야 한다.
6. RLHF는 “진실 학습”이 아니라 주로 “사람 선호에 맞는 행동 학습”이다. 정중함과 정확성을 혼동하면 안 된다.
7. Leaderboard 순위는 벤치마크, 프롬프트, 평가 버전, 모델 옵션과 날짜에 따라 달라진다. 특정 순위보다 자신의 실제 작업으로 평가해야 한다.
8. LLaMA를 포함한 공개 가중치 모델은 라이선스 제한이 있을 수 있으므로 `open source`라는 표현을 그대로 법적 의미로 받아들이면 안 된다.
9. Scaling Law는 계속 키우면 무조건 경제적으로 유리하다는 뜻이 아니다. 데이터 품질, 추론 비용, 전력, 지연시간, 배포 제약까지 함께 봐야 한다.
10. MoE의 큰 총 파라미터 수가 매 토큰마다 전부 계산된다는 뜻은 아니다. 반대로 활성 파라미터가 적다고 통신·라우팅 비용까지 항상 싼 것도 아니다.
11. Day 2 pp.35-36은 제목이 `Gemini 3.x`이지만 본문·표는 `Gemini 2.5 Pro/Flash/Flash-Lite`를 설명한다. 제목과 본문의 버전이 일치하지 않는다.
12. Day 2 p.60은 Distillation을 “모델 압축이 아니라 능력 전달”이라고 설명한 뒤 “모델 압축 및 지식 전달”이라고 정의한다. 정확히는 **Teacher-Student 지식 전달이 핵심이고, 작은 Student를 선택하면 압축 효과도 얻는다**고 이해하면 된다.
13. Day 2 p.61의 “추론 속도 향상”은 Soft Label 자체가 추론을 빠르게 해서라기보다, Soft Label로 학습한 작은 Student를 배포함으로써 얻는 효과다.
14. Day 2 p.57의 가격-성능 그래프는 특정 평가점수와 입력 토큰 가격만 비교한다. 출력 가격, 지연시간, 안전성, 실제 작업 성능 전체를 대표하지 않는다.
15. Day 2 p.58의 `전체 671B, 활성 37B` 표기는 **총 파라미터와 토큰당 활성 파라미터가 다르다**는 뜻이며 뒤의 MoE 설명과 직접 연결된다. 활성 계산은 줄어도 전체 Expert 가중치의 저장·통신 비용은 남는다.

---

## 5. 시험·면접용 핵심 답변

### 왜 Transformer가 RNN보다 LLM에 적합한가?

RNN은 토큰을 순차 처리해 병렬화가 어렵고 긴 거리 의존성 학습이 약하다. Transformer는 Self-Attention으로 모든 토큰 쌍의 관계를 직접 계산해 장거리 문맥을 다루기 쉽고, 학습 시 토큰 축 병렬화가 가능해 대규모 데이터·모델 학습에 적합하다.

### Q, K, V를 직관적으로 설명하면?

- Q는 내가 찾는 조건이다.
- K는 각 정보에 붙은 검색용 색인이다.
- `Q·K`는 얼마나 잘 맞는지를 나타내는 점수다.
- V는 실제로 가져올 내용이다.
- Softmax 가중치로 V들을 섞으면 현재 토큰의 문맥 표현이 된다.

### Multi-head가 필요한 이유는?

한 종류의 관계만 보는 대신 서로 다른 투영 공간에서 문법, 의미, 장거리 참조 등 여러 관계를 병렬로 학습하고 이를 하나의 표현으로 결합하기 위해서다.

### Positional Encoding이 필요한 이유는?

Self-Attention은 입력 순서를 바꿔도 같은 토큰 집합만 보면 순서를 자체적으로 알 수 없다. 토큰 위치 정보를 임베딩에 더해야 어순 차이를 학습할 수 있다.

### Causal mask가 필요한 이유는?

다음 토큰 모델은 현재 시점 이후의 정답을 보면 안 된다. 미래 위치의 Attention score를 차단해 각 위치가 과거와 현재 토큰만 보도록 한다.

### RLHF, Distillation, MoE의 차이는?

- RLHF: 모델의 **행동을 사람 선호에 맞춘다.**
- Distillation: 큰 모델의 **능력을 작은 모델에 전달한다.**
- MoE: 큰 모델 안에서 토큰마다 일부 Expert만 사용해 **연산을 희소화한다.**

### 좋은 LLM/Agent 포트폴리오는 무엇을 보여줘야 하나?

모델 데모 하나보다 `문제 정의 → 데이터 근거 → 시스템 설계 → 도구·권한·검증 → 운영 지표 → 실제 개선 효과`의 전 과정을 보여줘야 한다.

---

## 6. 마지막 암기 12문장

1. 언어모델은 앞선 토큰으로 다음 토큰의 확률분포를 예측한다.
2. One-hot과 BoW는 의미·어순을 충분히 표현하지 못한다.
3. 임베딩은 의미를 연속적인 벡터 공간에 표현한다.
4. 문맥 임베딩은 같은 단어도 문장에 따라 다른 벡터를 만든다.
5. `QK^T`는 어떤 토큰을 얼마나 볼지 계산한다.
6. `÷√d_k`는 큰 내적으로 인한 Softmax 포화를 완화한다.
7. Attention weight와 V의 곱은 관련 정보를 가중합한다.
8. Multi-head는 서로 다른 관계를 병렬로 학습한다.
9. Positional Encoding은 어순을, causal mask는 생성 방향을 보장한다.
10. Pretraining은 범용 능력을, RLHF는 사람 선호에 맞는 행동을 만든다.
11. Scaling은 파라미터·데이터·연산의 균형 문제다.
12. Distillation은 지식 전달, MoE는 선택적 Expert 활성화다.
